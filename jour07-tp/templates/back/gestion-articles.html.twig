{% extends "base.html.twig" %}

{% block title %}Gestions de Articles{% endblock %}


{% block body %}
    <h1>Gestion des articles</h1>

    <table class="table table-striped align-middle">
        <thead>
            <tr>
                <th>#id</th>
                <th>titre</th>
                <th>image</th>
                <th>description</th>
                <th>auteur</th>
                <th>action</th>
            </tr>
        </thead>
        <tbody>
            {% for article in articles %}
                <tr>
                    <td>{{article.id }}</td>
                    <td>{{article.titre }}</td>
                    <td><img src="{{ article.urlImg }}?{{article.titre }}" alt="" width="100" class="img-thumbnail"></td>
                    <td>{{article.description }}</td>
                    <td>{{article.auteur }}</td>
                    <td>
                        xxxx
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}